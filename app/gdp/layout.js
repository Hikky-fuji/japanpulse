export const metadata = {
  title: { absolute: 'Japan GDP Dashboard | JapanPulse' },
  description: 'Japan real GDP growth and demand-contribution dashboard using official Cabinet Office data.',
  alternates: { canonical: '/gdp' },
}

export default function GdpLayout({ children }) {
  return children
}

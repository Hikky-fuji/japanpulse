export const metadata = {
  title: { absolute: 'Japan Trade Dashboard | JapanPulse' },
  description: 'Japan exports, imports, trade balance, partner and product-flow dashboard using official customs data.',
  alternates: { canonical: '/trade' },
}

export default function TradeLayout({ children }) {
  return children
}

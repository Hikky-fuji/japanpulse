export const metadata = {
  title: { absolute: 'Japan Industrial Production | JapanPulse' },
  description: 'Japan industrial production, shipments, inventories, forecasts and inventory-cycle reference dashboard.',
  alternates: { canonical: '/iip' },
}

export default function IipLayout({ children }) {
  return children
}

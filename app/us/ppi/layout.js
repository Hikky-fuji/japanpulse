export const metadata = {
  title: {
    absolute: 'US PPI & Pipeline Inflation | JapanPulse',
  },
  description:
    'US headline and core producer prices, goods-services breadth and pipeline inflation momentum using BLS data via FRED.',
  alternates: {
    canonical: '/us/ppi',
  },
}

export default function UsPpiLayout({ children }) {
  return children
}

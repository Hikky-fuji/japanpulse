export const metadata = {
  title: { absolute: 'Japan Wages Dashboard | JapanPulse' },
  description: 'Japan nominal wages, real wages, scheduled pay and labor-income momentum dashboard.',
  alternates: { canonical: '/wages' },
}

export default function WagesLayout({ children }) {
  return children
}

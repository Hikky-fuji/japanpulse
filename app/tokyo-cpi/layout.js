export const metadata = {
  title: { absolute: 'Tokyo CPI Dashboard | JapanPulse' },
  description: 'Tokyo headline, core and core-core CPI dashboard, a leading indicator for Japan inflation.',
  alternates: { canonical: '/tokyo-cpi' },
}

export default function TokyoCpiLayout({ children }) {
  return children
}
